####################################################
#          ModuleBuilder Configuration             #
####################################################
VersionedOutputDirectory: true
BuiltModuleSubdirectory: module


####################################################
#          BinaryModuleBuildSettings Configuration #
####################################################

# -----------------------------------------------------------------------------
# This section defines build settings for a PowerShell binary module project.
# It controls:
#   • Solution/project structure (PrimaryProject, TargetFrameworks, etc.)
#   • Output behavior (versioned folder structure, output paths)
#   • DotNet build verbosity and configuration
#   • CopyOptions to include or exclude build artifacts (PDB, .deps.json)
# -----------------------------------------------------------------------------

BinaryModuleBuildSettings:
  VersionedOutputDirectory: false
  ProjectName: "LarryWisherMan.ApiUtils"
  BuiltModuleSubdirectory: "module"
  PrimaryProject: "Commands"
  TargetFrameworks:
    - netstandard2.0
  DotNetVerbosity: minimal
  Configuration: Release
  ModuleStagingSubdirectory: "bin/ModuleStaging"  # ← new
  CopyOptions:
    IncludePdb: false
    IncludeDepsJson: false






####################################################
#       Sampler Pipeline Configuration             #
####################################################
# Defining 'Workflows' (suite of InvokeBuild tasks) to be run using their alias
BuildWorkflow:
  '.':
    - build

  build:
    - Clean
    - DotNet_Clean
    - Build_BinaryModule

# Import ModuleBuilder tasks from a specific PowerShell module using the build
# task's alias. Wildcard * can be used to specify all tasks that has a similar
# prefix and or suffix. The module contain the task must be added as a required
# module in the file RequiredModules.psd1.
ModuleBuildTasks:
  Sampler:
    - '*.build.Sampler.ib.tasks'
  Sampler.GitHubTasks:
    - '*.ib.tasks'


# Invoke-Build Header to be used to 'decorate' the terminal output of the tasks.
TaskHeader: |
  param($Path)
  ""
  "=" * 79
  Write-Build Cyan "`t`t`t$($Task.Name.replace("_"," ").ToUpper())"
  Write-Build DarkGray  "$(Get-BuildSynopsis $Task)"
  "-" * 79
  Write-Build DarkGray "  $Path"
  Write-Build DarkGray "  $($Task.InvocationInfo.ScriptName):$($Task.InvocationInfo.ScriptLineNumber)"
  ""
